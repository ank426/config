(defwindow dim
  :monitor 0
  :stacking "overlay"
  :geometry (geometry :width "100%" :height "100%")
  (box :style "background: rgba(0, 0, 0, 0.5)")
)

;; (include "status/time.yuck")
;; (include "status/battery.yuck")
;; (include "status/niri_ws.yuck")
;; (include "status/calendar.yuck")
(include "status/audio.yuck")
